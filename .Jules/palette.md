## 2024-06-14 - Aria Labels in Dynamic Lists
**Learning:** Adding dynamic ARIA labels using item context (e.g., `aria-label={`Download ${output.name}`}`) significantly improves the screen reader experience over static strings.
**Action:** Always prefer context-specific strings for generic actions in mapped arrays.
