# Raw HTML is large and reproducible via scripts/fetch_docs.sh — keep out of git.
corpus/raw/

# Chunked corpora are regenerable from corpus/parsed/ via scripts/chunk_docs.py.
corpus/chunked/

# Build/runtime artifacts (embeddings, indexes, caches) — regenerated, not source.
embeddings/
indexes/
artifacts/
*.npz
*.bin

# Local toolchains
.venv/
__pycache__/
node_modules/

# transformers.js model cache + embedding artifacts
.cache/
package-lock.json
results/report_preview.png
results/*.png

# Generation-experiment run logs (regenerable; transcripts live in gen/results.json)
gen/run*.log
# Regenerable via gen/build_manual_prompts.mjs
gen/manual_prompts/
