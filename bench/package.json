{
  "name": "edge-rag-bench",
  "private": true,
  "type": "module",
  "description": "Edge RAG retrieval benchmark — Node + transformers.js (build == browser runtime).",
  "dependencies": {
    "@huggingface/transformers": "^3.3.0"
  }
}
